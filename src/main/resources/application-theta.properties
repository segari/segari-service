# Theta environment configuration
segari.backend.endpoint=https://theta-api-v2.segari.id
websocket.server.url=wss://theta-api-v2.segari.id

# Disable database encryption in non-production (machine-specific key)
segari.database.encryption.enabled=false
