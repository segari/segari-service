# Production environment configuration
segari.backend.endpoint=https://api-v2.segari.id
websocket.server.url=wss://api-v2.segari.id

# Enable database encryption in production (machine-specific key)
segari.database.encryption.enabled=true
